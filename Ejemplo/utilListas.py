def finDictionary (dataList, Key, value):
    info = {}
    for i in range(len(dataList)):
        if data list[i].get(key) == value:
            info["index"] = i
            info["data"] = dataList[i]
            break
    return info